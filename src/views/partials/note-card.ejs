<article class="note note-<%= note.color %>">
  <form action="/notes/<%= note.id %>" method="post" class="note-form">
    <input name="title" value="<%= note.title %>" maxlength="255" aria-label="Edit title">
    <textarea name="body" rows="5" aria-label="Edit body"><%= note.body %></textarea>
    <div class="note-footer">
      <select name="color" aria-label="Change color">
        <% colors.forEach((color) => { %>
          <option value="<%= color %>" <%= color === note.color ? "selected" : "" %>><%= color %></option>
        <% }) %>
      </select>
      <button type="submit">Save</button>
    </div>
  </form>

  <div class="note-actions">
    <% if (!note.is_archived) { %>
      <form action="/notes/<%= note.id %>/pin" method="post">
        <button type="submit"><%= note.is_pinned ? "Unpin" : "Pin" %></button>
      </form>
    <% } %>
    <form action="/notes/<%= note.id %>/archive" method="post">
      <button type="submit"><%= note.is_archived ? "Restore" : "Archive" %></button>
    </form>
    <form action="/notes/<%= note.id %>/delete" method="post">
      <button type="submit" class="danger">Delete</button>
    </form>
  </div>
</article>
